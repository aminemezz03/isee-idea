[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# API proxy + SPA redirects are written to dist/_redirects at build time
# (requires VITE_API_URL env var on Netlify)
